1.	List the minimum perks packages.
SELECT MIN(perks) from employee_data;
MIN(perks)
17000

2.	List the maximum salary given to a “Programmer”.
SELECT MAX(salary),title FROM employee_data WHERE title = 'Programmer';

3.	Display the age of the oldest “Marketing Executive”.
SELECT MAX(age), title FROM employee_data WHERE title = 'Marketing Executive';
MAX(age)	title
30	Marketing Executive

4.	Find the first and last names of the oldest employee.
SELECT f_name, l_name,MAx(age) FROM employee_data;
MAX(age)	title
30	Marketing Executive
